

To run the assignment place your input in the Same directory as assignment1.py. 
Make sure that the input is in format described as per the assignment question. 

While running the code pass the input test file as a command line argument. For example,
if the input test file is input.txt. To run the code sue the following command

`python assignment1.py input.txt`. If anything more the file path is passed using the command line
a ValueError is thrown.

The output will the generated in the same place as the input. 